import { CustomDescriptionProps } from "./CustomDescriptionProps";

export const CustomDescription = ({ description }: CustomDescriptionProps) => {
    return <div className={'default-description'}>{description}</div>
}